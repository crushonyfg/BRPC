# Documentation

- `paper/` contains the manuscript PDF.
- `notes/` contains archival design notes and Codex-assisted derivations/specs.
- `../rolled_cusum_modeling_workdoc.md` is the active modeling and runner log.

When changing discrepancy, restart, refresh, or runner semantics, update the
active workdoc in the same change.
