# AGENTS.md

`docs/` stores paper PDFs, design notes, and longer implementation specs.

- Keep `../rolled_cusum_modeling_workdoc.md` as the active modeling log unless it is deliberately migrated.
- Place archival notes under `docs/notes/`.
- Place paper/source PDFs under `docs/paper/`.
