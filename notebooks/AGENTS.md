# AGENTS.md

`notebooks/` contains exploratory notebooks.

- Do not make notebooks required for core installation or paper experiment reproduction.
- If a notebook result becomes part of the paper workflow, extract the runnable logic into `calib` or `tools`.
