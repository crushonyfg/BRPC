# AGENTS.md

`experiments/` is documentation for paper-level experiment families.

- Keep this folder focused on how to run experiments, not on implementation.
- Link to `calib` modules or `tools` scripts instead of duplicating logic.
- Add a short command, key flags, and expected outputs whenever a new experiment family is added.
