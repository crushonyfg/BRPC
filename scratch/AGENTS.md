# AGENTS.md

`scratch/` contains temporary scripts, one-off diagnostics, and historical patch helpers.

- Do not import from this folder in production code.
- Move reusable logic into `tools` or `calib`.
- Treat contents here as non-authoritative unless a current README or workdoc section references them.
