# Scratch

Temporary diagnostics and historical one-off scripts live here. They are kept
for traceability but are not part of the public API.

Reusable scripts should move to `../tools/`; algorithm code should move to
`../calib/`.
